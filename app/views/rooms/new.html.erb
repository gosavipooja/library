<%= form_for :room, url: rooms_path do |f| %>
 <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@room.errors.count, "error") %> prohibited
        this room from being saved:
      </h2>
      <ul>
        <% @room.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :Room_no %><br>
    <%= f.text_field :room_no %>
  </p>
 
  <p>
    <%= f.label :Size %><br>
    <%= f.select :size, [['Small-4','4'],['Medium-6','6'],['Large-12','12']] %>
  </p>
  
    <%= f.label :Building %><br>
    <%= f.select :building, ['DHHill','JamesHunt'] %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>